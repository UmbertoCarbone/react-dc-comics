export function DcComicsLinks({ title }) {
  return <a href="#">{title}</a>;
}

export function ShopLinks({ title }) {
  return <a href="#">{title}</a>;
}

export function DcLinks({ title }) {
  return <a href="#">{title}</a>;
}

export function SitesLinks({ title }) {
  return <a href="#">{title}</a>;
}

export function LinkBio({ img }) {
  return <img src={img} alt="" className="img.img" />;
}